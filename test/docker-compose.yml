version: '3.1'

services:
  mariadb:
    build: ./
    image: mariadb
    container_name: mariadb
    restart: always
    env_file:
      - ./.env
    volumes:
      - /home/pharbst/data/mariadb:/var/lib/mysql